/**
 * Probability reel palette. Shares the v3 ink and paper so the two lessons
 * read as the same channel; RED and BLUE do double duty here as both the
 * pen colours and the actual marble colours, which is deliberate - the
 * annotation colour always matches the thing it is describing.
 */
export {INK, BLUE, RED, TEAL, AMBER, MUTED, EASE} from '../scenes-v3/theme';

/** Kraft brown used for the bag, for captions that refer to it. */
export const KRAFT = '#B98A4B';
