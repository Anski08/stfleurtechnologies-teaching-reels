# dependencies
node_modules/

# rendered video output - the working directory for renders.
# Committing this would put every re-render permanently in history.
# Playback copies live in site/videos/ (Pages needs to serve them itself);
# distribution copies live in GitHub Releases.
out/

# vendored skill/reference docs pulled in locally
.claude/

# editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# remotion
.remotion/
